const path = require('path');

/**
 * Configuración para sequelize-cli (migraciones).
 * El runtime de la app usa su propia conexión en src/infrastructure/persistence/sequelize.ts.
 */
module.exports = {
  config: path.resolve('sequelize.config.cjs'),
  'migrations-path': path.resolve('migrations'),
  'seeders-path': path.resolve('seeders'),
};
